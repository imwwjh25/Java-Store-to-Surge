1. **字符串常量池**：编译期能确定的字面量（如 `"str_a"`、`"str_b"`）会存入常量池，相同字面量仅存一份；

2. 拼接规则

   ：

   - 纯字面量拼接（如 `"str_a" + "str_b"`）：编译期优化为 `"str_astr_b"`，直接存入常量池；
   - 含变量的拼接（如 `a + "str_b"`，a 是变量）：编译期无法优化，运行期通过 `StringBuilder` 拼接，生成新的堆字符串对象；

3. **new String(xxx)**：必然在堆上创建一个新 String 对象（即使参数是常量池字符串）。
